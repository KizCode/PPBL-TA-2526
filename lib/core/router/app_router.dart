import 'package:flutter/material.dart';

class AppRouter {
  static Route<dynamic> onGenerateRoute(RouteSettings settings) {
    // Wire routes here as features are implemented.
    return MaterialPageRoute(builder: (_) => const Placeholder());
  }
}
